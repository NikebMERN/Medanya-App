# Medanya Admin Panel
# Copy to .env and set values

# Backend API (if not using Vite proxy)
VITE_API_URL=http://localhost:4001

# Optional: override API base (defaults to /api with proxy in dev)
# VITE_API_BASE=/api
